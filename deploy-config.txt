# FTP Deployment Configuration
# Save your FTP credentials here - KEEP THIS FILE PRIVATE!
# Add to .gitignore so it's never committed to GitHub

# Your FTP server address
FTP_HOST=ftp.yourserver.com

# Your FTP username
FTP_USERNAME=your-ftp-username

# Your FTP password
FTP_PASSWORD=your-ftp-password

# Remote directory on FTP server (usually /public_html/)
FTP_REMOTE_DIR=/public_html/

# Local directory to sync (usually current directory)
FTP_LOCAL_DIR=.
